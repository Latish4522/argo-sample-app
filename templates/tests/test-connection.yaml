apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cron-basic.fullname" . }}-test-connection"
  labels:
    {{- include "cron-basic.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cron-basic.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
